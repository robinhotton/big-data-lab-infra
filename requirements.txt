# Dépendances des modules airflow/src/ et des DAGs.
# En conteneur : installées via _PIP_ADDITIONAL_REQUIREMENTS (voir docker-compose.yml).
# Ce fichier sert pour le lint / tests / exécution locale hors Docker.
#
# Python pur (boto3 + stdlib) — aligné sur CODE/ du cours : pas de pandas ni de Spark
# dans le code. pandas est installé PAR PRÉCAUTION (au cas où) mais jamais importé.

# Runtime (airflow/src + DAGs)
boto3==1.34.162
pandas==2.2.2  # par précaution seulement — le pipeline n'en fait pas usage

# --- Dev (tests / lint hors Docker) ---
pytest==8.3.2
ruff==0.6.3
